{
  "name": "n8n DevContainer",
  "dockerComposeFile": "docker-compose.yml",
  "service": "n8n",
  "workspaceFolder": "/workspace",
  "forwardPorts": [5678],
  "postCreateCommand": "git config --global user.name 'n8n-user' && git config --global user.email 'jaimehenao8126@outlook.com' && echo 'n8n DevContainer listo'",
  "customizations": {
    "vscode": {
      "settings": {
    "terminal.integrated.shell.linux": "/bin/bash"
      }
    }
  },
  "remoteUser": "node"
}
